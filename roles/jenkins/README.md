Role Name: jenkins
Description: Automates installation and configuration of Jenkins servers.
Features:
  - Cross-platform (Debian, RedHat, Amazon Linux)
  - Idempotent installation
  - Configurable via variables
  - Uses templates for dynamic Jenkins configuration
  - Handlers to restart service only when necessary
Usage:
  Include this role in your playbook and set variables as needed.

